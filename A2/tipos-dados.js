var nome = 'andre'; //string
var sobrenome = 'gomes';
var nomeCompleto = nome + ' ' + sobrenome;

console.log(nomeCompleto);

var idade = 28; //number
var possuiFaculdade = true; //boolean
var time; //undefined
var comida = null; //null
var simbolo = Symbol() //symbol
var novoObjeto = {} //object

console.log(typeof nome); // mostra o tipo de dado

//template strings
var gols = 1000;
var frase = `romário fez ${gols} gols na copa`;
console.log(frase);

